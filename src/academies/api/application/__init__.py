"""Application layer for the academies app.

Use cases, DTOs, and mappers for the academy and membership aggregates.
Imports only the domain layer (plus ``common``); never reaches into Django
or FastAPI.
"""
