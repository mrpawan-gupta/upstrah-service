"""Additional ports for the academies application layer.

No extra dispatcher/service ports are required yet (academies emit no
external side effects); this package reserves the layer slot so the
scaffold stays uniform across apps.
"""
